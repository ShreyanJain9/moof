(module workspace
  (requires collections geometry classes membrane json modules mcp bootstrap system)
  (unrestricted)
  (provides test-vau twice myfoo i))

(def test-vau (vau (name . body) $e (cons (quote def) (cons name body))))

(def twice (fn (x) [x * 2]))

(def myfoo 42)

(def i 0)
